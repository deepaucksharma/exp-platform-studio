[build-system]
requires = ["setuptools>=42.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.black]
line-length = 88
target-version = ["py38"]

[project]
name = "dstudio_implementation"
version = "0.1.0"
authors = [
    {name = "DStudio", email = "example@example.com"},
]
description = "DStudio Implementation"
requires-python = ">=3.8"
dependencies = []
